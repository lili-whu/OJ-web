import SideMenu from '@/components/SideMenu';
import React from 'react';

const Info: React.FC = () => {
  return (
    <div>
      <SideMenu />
      This is user info page. // todo添加一个user信息展示, 左边添加一个分栏
    </div>
  );
};

export default Info;
